import type * as siteSdkModuleContext from './wix-site-frontend.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const siteContext: Methods<typeof siteSdkModuleContext>;
export {};
