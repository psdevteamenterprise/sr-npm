import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as siteSdkModuleRuntime from './wix-site-frontend.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const siteRuntime: MaybeContext<HostModule<Methods<typeof siteSdkModuleRuntime>, Host> & Methods<typeof siteSdkModuleRuntime>>;
export {};
