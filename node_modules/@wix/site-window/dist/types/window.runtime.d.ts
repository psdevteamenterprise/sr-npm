import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as windowSdkModuleRuntime from './wix-window-frontend.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const windowRuntime: MaybeContext<HostModule<Methods<typeof windowSdkModuleRuntime>, Host> & Methods<typeof windowSdkModuleRuntime>>;
export {};
