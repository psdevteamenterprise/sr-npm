import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as renderingSdkModuleRuntime from './rendering.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const renderingRuntime: MaybeContext<HostModule<Methods<typeof renderingSdkModuleRuntime>, Host> & Methods<typeof renderingSdkModuleRuntime>>;
export {};
