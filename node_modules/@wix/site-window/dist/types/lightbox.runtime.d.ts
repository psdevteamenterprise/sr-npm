import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as lightboxSdkModuleRuntime from './lightbox.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const lightboxRuntime: MaybeContext<HostModule<Methods<typeof lightboxSdkModuleRuntime>, Host> & Methods<typeof lightboxSdkModuleRuntime>>;
export {};
