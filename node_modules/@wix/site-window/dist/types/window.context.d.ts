import type * as windowSdkModuleContext from './wix-window-frontend.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const windowContext: Methods<typeof windowSdkModuleContext>;
export {};
