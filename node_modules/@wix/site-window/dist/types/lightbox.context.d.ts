import type * as lightboxSdkModuleContext from './lightbox.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const lightboxContext: Methods<typeof lightboxSdkModuleContext>;
export {};
