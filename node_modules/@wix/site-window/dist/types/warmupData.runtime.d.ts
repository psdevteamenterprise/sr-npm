import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as warmupDataSdkModuleRuntime from './warmupdata.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const warmupDataRuntime: MaybeContext<HostModule<Methods<typeof warmupDataSdkModuleRuntime>, Host> & Methods<typeof warmupDataSdkModuleRuntime>>;
export {};
