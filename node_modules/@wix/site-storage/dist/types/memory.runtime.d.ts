import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as memorySdkModuleRuntime from './storage.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const memoryRuntime: MaybeContext<HostModule<Methods<typeof memorySdkModuleRuntime>, Host> & Methods<typeof memorySdkModuleRuntime>>;
export {};
