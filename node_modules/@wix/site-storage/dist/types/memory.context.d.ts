import type * as memorySdkModuleContext from './storage.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const memoryContext: Methods<typeof memorySdkModuleContext>;
export {};
